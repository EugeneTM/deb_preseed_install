DEFAULT {{ preseed.os_name }}

LABEL {{ preseed.os_name }}
	kernel linux
	append auto=true initrd=initrd.gz debian-installer/language=ru astra-license/license=true net.ifnames=0 {% if host is defined %}hostname={{ host.hostname | default('any') }} domain={{ default_domain.upper() }} url={{ server_type }}://{{ network.addr }}/preseeds/{{ preseed.name }} interface={{ preseed.interface }} {% if host.ip is defined and host.ip %}netcfg/get_ipaddress={{ host.ip }} netcfg/get_netmask={{ host.mask | default(network.mask)}} netcfg/get_gateway={{ network.addr }} netcfg/get_nameservers={{ network.addr }} netcfg/disable_dhcp=true{% else %}netcfg/choose_interface=auto netcfg/dhcp_timeout=60{% endif %}{% else %}hostname=any domain={{ default_domain.upper() }} url={{ server_type }}://{{ network.addr }}/preseeds/{{ default_preseed }} interface={{ preseed.interface }} netcfg/choose_interface=auto netcfg/dhcp_timeout=60{% endif %}

TIMEOUT 1
